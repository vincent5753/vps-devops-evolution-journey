# cp .env.example .env   (.env is gitignored)
# set -a; . ./.env; set +a

# Token for AnsibleUser@pve!inventory, created by
# ../02-Forgejo-Host-Prerequisites/00-pve-ansible-token.sh.
# Not the OpenTofu token: that one can create and destroy VMs.
PROXMOX_TOKEN_SECRET=00000000-0000-0000-0000-000000000000
