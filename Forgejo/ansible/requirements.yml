# ansible-galaxy collection install -r requirements.yml -p collections
---
collections:
  # The Proxmox inventory plugin. NOT community.general.proxmox: that
  # content is deprecated and removed in community.general 15.0.0.
  - name: community.proxmox

  # ansible.posix.sysctl — not in ansible-core, and the usual first trip-up.
  - name: ansible.posix

  - name: community.postgresql
