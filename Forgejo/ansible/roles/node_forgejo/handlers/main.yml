---
- name: Apply netplan
  ansible.builtin.command: netplan apply
  changed_when: true

- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart forgejo
  ansible.builtin.systemd_service:
    name: forgejo
    state: restarted
